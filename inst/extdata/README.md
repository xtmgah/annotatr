## Description of `inst/extdata/`

This folder contains raw data that we use in the vignettes to demonstrate a user workflow. We can also put data in here that are used for unit tests in `tests/testthat/`.
